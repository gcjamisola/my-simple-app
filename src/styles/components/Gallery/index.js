import styled from 'styled-components';

const GalleryContainer = styled.div`
	display: flex;
	flex-wrap: wrap;
`;

const GalleryImage = styled.img`
	width: 10%;
`;

export { GalleryContainer, GalleryImage };